git add .
git commit -m "update footer, add version and updatedAt to footer, add rate limit on check or sync"
git tag -a v3.2.5 -m "update footer, add version and updatedAt to footer, add rate limit on check or sync"
git push github main
git push github --tags